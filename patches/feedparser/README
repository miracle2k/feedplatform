Patches to Universal Feed Parser. The builtin version of
``feedparser.py`` has them already applied.

Not all are strictly required. More information is included with each
patch file.

The patches are managed using Stacked Git.